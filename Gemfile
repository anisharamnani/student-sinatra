source 'https://rubygems.org'

gem 'sinatra', require: 'sinatra/base'

group :development do 
	gem 'shotgun' # allows you to not have to restart your local server 
	gem 'tux' # allows us to have a console to interact with our application 
end
